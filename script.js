// let count = 1;
// document.getElementById("radio1").checked = true;

// setInterval(function() {
//   nextImage();
// }, 2000);

// function nextImage() {
//   count++;
//   if (count > 4) {
//     count = 1;
//   }

//   document.getElementById("radio" + count).checked = true;
// }

// const manualBtns = document.querySelectorAll('.manual-btn');

// manualBtns.forEach(btn => {
//   btn.addEventListener('click', (event) => {
//     // Navigate to the desired section based on the button text or ID
//     const sectionId = event.target.textContent.toLowerCase().replace(/\s+/g, '-'); // Convert spaces to hyphens
//     const targetSection = document.getElementById(sectionId);
//     if (targetSection) {
//       targetSection.scrollIntoView({ behavior: 'smooth' }); // Smooth scrolling
//     } else {
//       console.error(`Section with ID "${sectionId}" not found`);
//     }
//   });
// });
